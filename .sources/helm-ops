https://github.com/chrishuffman5/domain-expert/tree/main/plugins/containers/skills/helm (MIT, sole source) -> SKILL.md + references/{architecture,best-practices}.md re-authored into helm-ops/... (Helm chart authoring and release operations: chart structure, Go/Sprig templating, values + JSON schema, release lifecycle, dependencies/OCI, hooks, testing/linting, Helmfile, helm-secrets/SOPS, Helm 3 to 4 SSA)

--- Chunk C Containers family PR 2 of 3 (orchestration), 2026-08-04 ---
NEW helm-ops vendor skill. MIT (chris `helm` sole source). Owns Helm chart authoring/release operations; the cluster it deploys onto routes to kubernetes-ops (same PR); orchestrator choice to container-orchestration-selection; SECURITY (chart/image scanning, admission, supply chain) to container-security; the secret backend behind helm-secrets to hashicorp-vault-ops/secrets-hygiene; GitOps CD (ArgoCD/Flux) to cicd-platforms-ops/gh-actions-ci; Terraform Helm provider to terraform-iac-ops; mesh to service-mesh-selection (forward ref, PR3). Customisations: chris agent-persona framing stripped; upstream Helm 4 "4.1.x" over-claim tempered to "GA late 2025"; decorative Unicode flow arrows converted to ASCII; references headerless; British/Pacific English; no em/en dashes (byte-scan == 0).
