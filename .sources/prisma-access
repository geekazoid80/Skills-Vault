https://github.com/chrishuffman5/domain-expert/tree/main/plugins/security/skills/prisma-access (MIT, primary) -> SKILL.md + references/architecture.md re-authored into prisma-access/SKILL.md + references/architecture.md + operations.md (compute-location fabric, PAN-OS pipeline, App-ID/Content-ID, GlobalProtect + HIP, ZTNA 2.0 engine + service connections, Panorama vs Strata Cloud Manager, Cortex Data Lake, WildFire, ADEM)
https://github.com/vahagn-madatyan/netsec-skills-suite/tree/main/skills/prisma-access-audit (Apache-2.0, secondary attributed) -> references/api-reference.md genericised into references/api-and-automation.md + the read-only audit flow in operations.md (SCM OAuth 2.0 + legacy Panorama API key, config-API endpoints, Insights query API, pagination, error codes, rate limits)

--- Chunk C Email + SASE/SSE family PR 2 of 4 (prisma-access vendor skill), 2026-07-30 ---
Per-vendor Palo Alto Prisma Access (cloud SASE) ops skill (ZTNA 2.0, FWaaS, ADEM). MIT primary (chris ops/architecture), vahagn Apache-2.0 secondary attributed. Routes SASE/SSE + zero-trust DESIGN / assessment / selection UP to sase-sse; owns Prisma Access configuration and operations. BOUNDARY: Prisma Access (cloud SASE) is distinct from on-prem PAN-OS firewalls; on-prem firewall auditing routes to palo-alto-firewall-audit, not duplicated here. Authored by a delegated subagent, then reviewed and gated by the parent session.
Customisations: agent-persona framing + openclaw metadata + MCP plumbing stripped; upstream names not carried; references headerless; API examples use placeholder tokens only; em-dash / en-dash purge (Python byte-scan == 0); British / Pacific English; ASCII -> arrows.
