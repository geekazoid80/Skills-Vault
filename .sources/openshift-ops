https://github.com/chrishuffman5/domain-expert/tree/main/plugins/containers/skills/openshift (MIT, sole source) -> SKILL.md + references/architecture.md re-authored into openshift-ops/... (Red Hat OpenShift/OKD operations: Projects, Routes, SCC vs Pod Security, BuildConfigs/S2I, ImageStreams, OLM/operators, DeploymentConfig, Machine Config Operator, oc CLI)

--- Chunk C Containers family PR 2 of 3 (orchestration), 2026-08-04 ---
NEW openshift-ops vendor skill. MIT (chris `openshift` sole source). Owns OpenShift-specific operation; generic Kubernetes depth routes to kubernetes-ops (same PR, with the divergences made explicit: Routes vs Ingress, SCC vs Pod Security Standards, DeploymentConfig vs Deployment); orchestrator choice to container-orchestration-selection; SECURITY depth (image scanning, admission, supply chain) to container-security; secrets to hashicorp-vault-ops/secrets-hygiene; deep Prometheus config to prometheus-configuration/grafana-dashboards (OpenShift bundles a monitoring stack); CD to cicd-platforms-ops/gh-actions-ci; mesh (OpenShift Service Mesh is Istio-based) to service-mesh-selection (forward ref, PR3). Customisations: chris agent-persona framing stripped; corrected two upstream errors (DeploymentConfig deprecated since OCP 4.14; built-in Grafana removed from cluster monitoring in 4.11, stack is Prometheus/Alertmanager/Thanos); added version-boundary facts (OVN-Kubernetes default since 4.12, SDN removed 4.17, restricted-v2 default since 4.11); references headerless; British/Pacific English; no em/en dashes (byte-scan == 0).
