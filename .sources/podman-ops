https://github.com/chrishuffman5/domain-expert/tree/main/plugins/containers/skills/podman (MIT, sole source) -> SKILL.md + references/{architecture.md,best-practices.md,versions/6.0.md} re-authored into podman-ops/SKILL.md + references/{architecture.md,best-practices.md} (upstream versions/6.0 absorbed into the SKILL.md Version notes section). Podman daemonless/rootless-by-default ops across 5.x-6.0: rootless setup subuid/subgid + userns, Quadlet systemd (.container/.pod/.network/.volume), K8s-compatible pods + generate/play kube, podman machine on macOS/Windows, Buildah, Netavark + Aardvark-DNS + pasta networking, crun, SELinux :z/:Z volume labels, podman-compose + Docker-socket compat.

--- Chunk C Containers family PR 1 of 3 (runtimes), 2026-08-03 ---
NEW Podman ops vendor skill. MIT (chris `podman` sole source). Runtime SELECTION + Podman-vs-Docker comparison + Docker-to-Podman migration strategy routed OUT to container-runtime-selection (the upstream "Key Differences from Docker" table + phased migration guide deliberately not carried; only day-to-day Docker-socket compat kept as ops); siblings docker-ops / containerd-ops; security strategy to container-security (Podman rootless/userns ops knobs kept); Kubernetes orchestration + CRI-O to container-orchestration-selection (forward ref, PR2); certs/secrets to cert-manager / secrets-hygiene / hashicorp-vault-ops; ingress to nginx-load-balancing / load-balancer-selection; CI/CD to cicd-platforms-ops / gh-actions-ci; observability to prometheus-configuration / grafana-dashboards. RHEL host layer noted out of scope (OS block). Customisations: chris persona framing stripped; references headerless; British/Pacific English; no dashes.
