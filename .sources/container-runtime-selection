https://github.com/chrishuffman5/domain-expert/tree/main/plugins/containers/skills/runtimes (MIT, sole source) -> SKILL.md + references/concepts.md re-authored into container-runtime-selection/SKILL.md + references/concepts.md (runtime comparison Docker/Podman/containerd execution models, daemon vs daemonless, decision matrix, OCI runtime layer runc/crun/youki/gVisor/Kata, migration patterns Docker-to-Podman + Docker-to-containerd, OCI spec + namespaces + cgroups + union filesystems + image format + registries)

--- Chunk C Containers family PR 1 of 3 (runtimes), 2026-08-03 ---
NEW vendor-neutral container-runtime SELECTION umbrella. MIT (chris `runtimes` sole source). First of the three selection umbrellas for the Containers slice (runtime / orchestration / service-mesh), matching the vault umbrella+vendor pattern (cloud-platform-selection, load-balancer-selection, network-monitoring-selection). Owns runtime comparison/selection/migration reasoning ONLY; deep per-runtime implementation routes OUT to the sibling vendor skills docker-ops / podman-ops / containerd-ops (same PR). Boundary: container/Kubernetes SECURITY routes to container-security; cluster-level orchestration + CRI choice routes to container-orchestration-selection (forward ref, lands PR2 this session); observability to prometheus-configuration / grafana-dashboards. Customisations: chris agent-persona framing stripped; references headerless; image-signing pointed at container-security; ACR "AAD auth" modernised to "Entra ID auth"; British/Pacific English; no em/en dashes (Python byte-scan == 0).
