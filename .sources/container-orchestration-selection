https://github.com/chrishuffman5/domain-expert/tree/main/plugins/containers/skills/orchestration (MIT, primary) + .../overview (MIT, cross-platform framing) -> SKILL.md + references/concepts.md re-authored into container-orchestration-selection/SKILL.md + references/concepts.md (orchestration platform selection: self-managed vs managed Kubernetes vs OpenShift vs Rancher vs lightweight/edge; EKS/AKS/GKE comparison table; decision matrix; orchestration fundamentals desired-state/reconciliation/operators/CRDs/admission control)

--- Chunk C Containers family PR 2 of 3 (orchestration), 2026-08-04 ---
NEW vendor-neutral container-orchestration SELECTION umbrella. MIT (chris `orchestration` primary + `overview` for cross-platform framing). Second of the three selection umbrellas (runtime [PR1] / orchestration / service-mesh [PR3]). Owns the self-managed-vs-managed decision AND the which-managed-provider decision (keeps the EKS/AKS/GKE comparison table); deep per-platform implementation routes OUT to kubernetes-ops / helm-ops / openshift-ops / rancher-ops (same PR); managed-provider control-plane/IAM/node-pool DEPTH routes to aws-cloud-ops / azure-cloud-ops / gcp-cloud-ops (no standalone eks/aks/gke skills, mirrors the cloud-network "ops slots pre-empted" collapse); SECURITY to container-security; mesh to service-mesh-selection (forward ref, lands PR3). Customisations: chris agent-persona framing stripped; references headerless; British/Pacific English; no em/en dashes (Python byte-scan == 0).
