https://github.com/chrishuffman5/domain-expert/tree/main/plugins/containers/skills/kubernetes (MIT, sole source) -> SKILL.md + references/{architecture,best-practices,diagnostics}.md + references/versions/{1.34,1.35}.md re-authored into kubernetes-ops/... (day-to-day Kubernetes operations: control/data plane internals, workloads, scheduling, networking, storage, RBAC mechanics, autoscaling, kubeadm upgrades, kubectl troubleshooting; version notes 1.33-1.36)

--- Chunk C Containers family PR 2 of 3 (orchestration), 2026-08-04 ---
NEW kubernetes-ops vendor skill. MIT (chris `kubernetes` sole source). Owns generic Kubernetes operation; SELECTION/comparison routes to container-orchestration-selection (umbrella, same PR); SECURITY (Pod Security, admission control, network policy, image scanning, supply chain, RBAC-as-control) routes to container-security; managed-provider depth EKS/AKS/GKE to aws/azure/gcp-cloud-ops; ingress to load-balancer-selection + aws/nginx/haproxy-load-balancing; certs to cert-manager/lets-encrypt; secrets to hashicorp-vault-ops/secrets-hygiene; observability to prometheus-configuration/grafana-dashboards/distributed-tracing; GitOps/CD to cicd-platforms-ops/gh-actions-ci; IaC to terraform-iac-ops; mesh to service-mesh-selection (forward ref, PR3). Customisations: chris agent-persona + "How to Approach Tasks" framing stripped; upstream diagnostic scripts folded into references/diagnostics.md (no scripts/ dir, matches landed vendor shape); references headerless; British/Pacific English; no em/en dashes (byte-scan == 0). Corrected two upstream version-boundary errors: native sidecar containers GA is 1.33 (not 1.34); nftables kube-proxy is beta since 1.31 and GA in 1.33 (not "beta from 1.33"); plus VolumeAttributesClass GA example apiVersion v1 (not v1beta1) and a 1.35 enhancement-count arithmetic fix.
