https://github.com/chrishuffman5/domain-expert/tree/main/plugins/containers/skills/containerd (MIT, sole source) -> SKILL.md + references/architecture.md re-authored into containerd-ops/SKILL.md + references/architecture.md (containerd ops for Linux + Kubernetes nodes: CRI config.toml v3 for the kubelet sandbox_image/SystemdCgroup/runtime-classes runc/Kata/gVisor, snapshotters overlayfs/stargz/nydus/btrfs/zfs, namespaces moby-vs-k8s.io-vs-default, NRI plugin framework, registry mirror certs.d, garbage collection + leases, nerdctl/ctr/crictl CLIs)

--- Chunk C Containers family PR 1 of 3 (runtimes), 2026-08-03 ---
NEW containerd ops vendor skill. MIT (chris `containerd` sole source). Runtime SELECTION + containerd-vs-Docker comparison routed OUT to container-runtime-selection; siblings docker-ops (containerd sits beneath Docker's stack via the moby namespace) / podman-ops; Kubernetes cluster operations + orchestrator-level CRI decision to container-orchestration-selection (forward ref, PR2) with node-level CRI config kept here and the dockershim-removed-since-1.24 note; container/Kubernetes security + image signing/supply-chain to container-security; image building at scale to external BuildKit / cicd-platforms-ops; observability to prometheus-configuration / grafana-dashboards. Customisations: chris persona framing stripped; references headerless; British/Pacific English; no dashes.
