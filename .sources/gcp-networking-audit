https://github.com/vahagn-madatyan/netsec-skills-suite/tree/main/skills/gcp-networking-audit (Apache-2.0, sole source) -> SKILL.md + references/vpc-architecture.md + references/cli-reference.md re-authored into gcp-networking-audit/SKILL.md + references/cli-reference.md (read-only GCP VPC networking audit: 6-step procedure VPC network inventory/design, firewall rule audit, Cloud NAT + egress analysis, connectivity analysis Cloud Interconnect/Shared VPC/peering, Cloud Router + routing validation, report + optimisation; global-VPC-with-regional-subnets model, firewall priority + hierarchical firewall policy evaluation order, Cloud NAT port allocation, Shared VPC host/service model, Cloud Router BGP + dynamic routing mode, VPC peering constraints; three threshold tables, two decision trees, report template, troubleshooting, nine-element response contract; read-only `gcloud compute` throughout)

--- Chunk C cloud-network family PR 2 of 2, 2026-08-02 ---
NEW GCP VPC networking audit, multi-cloud parity sibling of the shipped aws-networking-audit. Apache-2.0 single-source (vahagn gcp-networking-audit). Mirrors aws-networking-audit's structure section-for-section. vpc-architecture.md folded into the SKILL.md body; cli-reference.md kept + cleaned. Authored by a delegated subagent, then independently gated by the parent (yaml.safe_load 4-field Apache-2.0 v1.0.0, Python dash byte-scan == 0 both files, marker present, cli-reference headerless, no persona framing, all backtick cross-refs resolve on disk). Boundary: audits the running GCP network plane (firewall/routes/NAT/connectivity); routes cross-cloud design UP to cloud-network-design, CSPM control-catalogue to cloud-security-posture, cloud selection to cloud-platform-selection. Cross-refs the aws + azure parity siblings (the trio landed together in PR2). The source vpc-architecture.md had no peering section; a GCP-accurate VPC Network Peering constraints table was authored per the fold rule (non-transitive, no overlap, both sides must peer, subnet/alias routes auto-exchanged, no tag/SA propagation, no Private Google Access transit). Customisations: agent-persona + upstream safety/openclaw/author frontmatter stripped; four-field frontmatter (license Apache-2.0) + skill marker; description re-quoted as a double-quoted single-line YAML scalar (loader-safe); references headerless; em/en-dash purge (Python byte-scan == 0); US-to-UK spelling.
