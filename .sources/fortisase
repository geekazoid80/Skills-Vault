https://github.com/vahagn-madatyan/netsec-skills-suite/tree/main/skills/fortisase-audit (Apache-2.0, sole source) -> SKILL.md + references/api-reference.md re-authored into fortisase/SKILL.md + references/architecture.md + operations.md + api-and-automation.md (FortiSASE fabric + PoPs, FortiClient + EMS posture model, secure private access ZTNA access proxy, secure internet access SWG + FWaaS, thin edge + SD-WAN convergence, SSL/TLS inspection, FortiGuard, FortiAnalyzer Cloud, FortiCloud IAM bearer-token API + FortiOS CMDB/monitor endpoints, read-only audit flow)

--- Chunk C Email + SASE/SSE family PR 2 of 4 (fortisase vendor skill), 2026-07-30 ---
Per-vendor Fortinet FortiSASE ops skill. Apache-2.0 (single Apache source; no chris FortiSASE content exists). Some vendor-accurate depth authored beyond the single source where it was thin (FortiSASE product features only, nothing invented). Routes SASE/SSE + zero-trust DESIGN / assessment / selection UP to sase-sse; owns FortiSASE configuration and operations. BOUNDARY: FortiSASE (cloud SASE) is distinct from on-prem FortiGate; on-prem FortiGate firewall auditing routes to fortigate-firewall-audit, not duplicated here. Authored by a delegated subagent, then reviewed and gated by the parent session.
Customisations: agent-persona framing + openclaw metadata + MCP plumbing stripped; upstream name not carried; references headerless; API examples use placeholder tokens only; em-dash / en-dash purge (Python byte-scan == 0); British / Pacific English; ASCII -> arrows.
