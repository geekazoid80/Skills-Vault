https://github.com/vahagn-madatyan/netsec-skills-suite/tree/main/skills/aws-networking-audit (customised; em dashes removed; British / Pacific English; AWS-only flat vendor surface, no sub-tags; `references/vpc-architecture.md` fully folded into SKILL.md body: packet flow diagrams into Step 2, SG-vs-NACL comparison into Step 2, Transit Gateway routing model into Step 3, VPC peering constraints + Peering-vs-TGW comparison into Step 3, subnet routing patterns into Step 5; upstream file deleted at adoption; `references/cli-reference.md` kept and extended with a credential-probe section bridging to `secrets-hygiene` "Probing the credential store"; upstream `safety` / `openclaw` / `metadata.author` frontmatter fields dropped per vault four-field House style; description rewritten for vault Claude-Search-Optimisation discipline with 49 quoted trigger phrases plus unquoted noun-phrase surfaces; nine-element response contract mapping to `multi-vendor-network-ops` added; cross-link surface extended to 12 live vault skills plus 4 forward `(when adopted)` placeholders for C-4c through C-4f; see merged-skills-registry)
