Primary source: chrishuffman5/domain-expert (MIT)
URL: https://github.com/chrishuffman5/domain-expert
This is the sole content source for windows-dns-ops. All SKILL.md body and reference material
derives from plugins/networking/skills/windows-dns/. MIT licence governs the SKILL.md and both
references.

Files folded -> reference mapping:
  plugins/networking/skills/windows-dns/SKILL.md
    -> SKILL.md (umbrella body: quick classification, key PowerShell cmdlet summary, event IDs,
       when to use, when not to use, cross-references, red flags, bottom line)
  plugins/networking/skills/windows-dns/references/architecture.md
    -> references/architecture.md (AD-integrated zones, replication scopes, zone types, forwarders,
       DNS policies and zone scopes, DNSSEC on Windows, aging/scavenging mechanics)
  plugins/networking/skills/windows-dns/references/best-practices.md
    -> references/best-practices.md (forwarder design, secure dynamic updates, scavenging config,
       DNSSEC operational guidance, split-brain via policies, monitoring, security hardening,
       upgrade and migration guidance)
  plugins/networking/skills/windows-dns/references/versions/2022.md
    -> references/architecture.md "Version notes" section: Server 2022 (client-side DoH, DNSSEC
       key storage, Azure Arc integration) absorbed; version-subfolder approach from upstream
       replaced with a single "Version notes (Server 2022 / 2025)" section.
  plugins/networking/skills/windows-dns/references/versions/2025.md
    -> references/architecture.md "Version notes" section: Server 2025 (server-side DoH public
       preview via KB5075899, DoH limitations, firewall consideration) absorbed into the same
       section.

Customisations applied:
  - Re-authored agent-style upstream ("You are a specialist", "How to Approach Tasks")
    into vault operator-facing shape: keyword-rich CSO description with boundary callout;
    four-field vault frontmatter (name, description, license MIT, metadata.version 1.0.0);
    skill marker in SKILL.md only.
  - References carry no frontmatter and no skill marker; line 1 is the # Title in each.
  - Version SKILLs (2022/SKILL.md and 2025/SKILL.md) absorbed into
    references/architecture.md as a "Version notes (Server 2022 / 2025)" section; no
    version subfolders created in the vault skill.
  - Upstream "--" dashes and em-dash-style pauses converted to commas, semicolons, and
    parentheses throughout. Em-dash (U+2014) and en-dash (U+2013): zero occurrences.
  - British/Pacific English applied: initialisation, authorised, behaviour, licence,
    optimise, organisation, centre, recognise.
  - Upstream "PowerShell only -- no GUI" framing reworded to operator prose.
  - Added cross-references to systematic-debugging and network-source-of-truth (not in
    upstream); removed entra-app-lifecycle (not relevant to DNS operations).
  - Extended scavenging red flags with "DHCP lease duration + 1 day" rule and the
    dnscmd /ageallrecords danger note; drawn from upstream pitfalls.
  - DoH section in architecture.md includes explicit callout that upstream forwarder queries
    remain unencrypted in Server 2025 preview, and firewall/TLS-certificate considerations.

Boundary notes:
  windows-dns-ops = Windows DNS SERVER IMPLEMENTATION: zones, forwarders, policies, scavenging,
  DNSSEC key operations, PowerShell automation, event diagnostics.
  dns-network-ops owns DNS ARCHITECTURE + CROSS-PLATFORM CONCEPTS (resolution flow, record types,
  DNSSEC design, platform selection, hybrid architecture). No content duplication: architecture
  decisions route to dns-network-ops; Windows-specific implementation lives here.
  Active Directory domain-services design (site topology, trust relationships, forest/domain
  structure) is out of scope for both skills.
  network-source-of-truth owns IPAM and the DNS record-of-intent.

Cross-refs added to vault skills: dns-network-ops, multi-vendor-network-ops, secrets-hygiene,
utc-timestamps, oncall-runbooks, systematic-debugging, network-source-of-truth.

Part of Chunk C DNS family PR2 (authoritative servers): vault 105 -> 108 across
bind-dns-ops + powerdns-ops + windows-dns-ops.
